from django.contrib import admin
from django.urls import path,include

from userapp import views

urlpatterns = [
    # path('get-blogs',views.get_blogs),
    # path('get-blog-fields',views.blogFieldValidation),

]   